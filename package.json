{
  "name": "evogr-site",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173 --host",
    "build": "rm -rf dist && vite build",
    "preview": "vite preview --outDir dist"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
